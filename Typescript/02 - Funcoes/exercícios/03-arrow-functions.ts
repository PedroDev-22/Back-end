const dobrar = (numero: number): number => numero * 2;

const saudar = (nome: string): string => `Olá, ${nome}`;

const ehPar = (numero: number): boolean => numero % 2 === 0;

console.log(dobrar(3));
console.log(saudar("Pedro"));
console.log(ehPar(6));